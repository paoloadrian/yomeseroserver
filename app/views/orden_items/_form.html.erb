<%= form_for(@orden_item) do |f| %>
  <% if @orden_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@orden_item.errors.count, "error") %> prohibited this orden_item from being saved:</h2>

      <ul>
      <% @orden_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cantidad %><br>
    <%= f.number_field :cantidad %>
  </div>
  <div class="field">
    <%= f.label :id_item %><br>
    <%= f.number_field :id_item %>
  </div>
  <div class="field">
    <%= f.label :id_orden %><br>
    <%= f.number_field :id_orden %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
